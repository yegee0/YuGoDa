# Data + DB
psycopg2-binary>=2.9
faker>=25.0
numpy>=1.24
pandas>=2.0
python-dotenv>=1.0

# Training (RTX 5070 Ti / Blackwell: torch>=2.5, CUDA 12.6+, may need nightly)
# Windows native install:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
# WSL2 + CUDA: same, plus configure WSL CUDA passthrough
torch>=2.5
sentence-transformers>=3.0
transformers>=4.40
tqdm>=4.66

# Export & inference
onnx>=1.16
onnxruntime>=1.18

# Plots (optional)
matplotlib>=3.7
